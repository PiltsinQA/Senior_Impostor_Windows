```markdown
# 🎯 Interview Assistant (Stealth Edition)

Интеллектуальный помощник для прохождения технических собеседований.  
Распознаёт речь в реальном времени, анализирует экран и **автоматически скрывается** при демонстрации экрана в Zoom, Discord, Skype и других платформах.

> 💡 Идеально подходит для кандидатов, которые хотят иметь «умные подсказки» под рукой — незаметно и безопасно.

---

## ✨ Основные возможности

- **🛡️ Stealth Mode (Скрытность)**  
  Окно программы **не отображается** при захвате экрана или создании скриншотов — остаётся невидимым для рекрутера.

- **🎙️ Автоматический режим**  
  Программа постоянно слушает микрофон и каждые **15 секунд** отправляет расшифрованный текст в ИИ для получения кратких, точных подсказок.

- **📸 Анализ экрана**  
  Сделайте мгновенный скриншот (например, с задачей по коду или архитектурной схемой) — и получите разбор от ИИ одним кликом.

- **🧠 Умное распознавание речи**  
  Использует локальную модель **`faster-whisper`** — ваша речь обрабатывается **на компьютере**, без отправки на внешние серверы.

---

## 🛠️ Установка

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/PiltsinQA/Senior_Impostor_Windows.git
   cd Senior_Impostor_Windows
   ```

2. Создайте виртуальное окружение:
   ```bash
   python -m venv .venv
   # Windows:
   .venv\Scripts\activate
   # macOS / Linux:
   source .venv/bin/activate
   ```

3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```


---

## ⌨️ Инструкция по работе

### 🔧 Настройка
1. Перейдите во вкладку **⚙️ Settings**.
2. Введите ваш **API Key** от [OpenRouter](https://openrouter.ai/).
3. Выберите модель Whisper:
   - `base` — рекомендуется (оптимальный баланс скорости и точности)
   - `small`, `medium` — для большей точности (требует больше ресурсов)
4. Задайте **System Prompt**, например:  
   > *"Ты — Senior Python Developer. Помогай мне на интервью: отвечай кратко, по делу, без воды."*

### ▶️ Работа в чате
- **🎙️ МИК** — начать запись вручную. Повторное нажатие отправит аудио на расшифровку.
- **🤖 АВТО** — включить циклический режим (автообработка каждые 15 сек).
- **📸 SCREEN** — сделать скриншот и запросить анализ у ИИ.

> ⚠️ **macOS**: При первом запуске разрешите доступ к **Микрофону** и **Записи экрана** в  
> *Системные настройки → Конфиденциальность и безопасность*.

---

## 📦 Зависимости (`requirements.txt`)

Убедитесь, что в корне проекта есть файл `requirements.txt` со следующим содержимым:

```
faster-whisper>=1.0.0
openai>=1.0.0
pyaudio>=0.2.13
pillow>=10.0.0
PyQt5>=5.15.0
python-dotenv>=1.0.0
```

> Для macOS также требуются:
> ```
> pyobjc-framework-Cocoa
> pyobjc-framework-Quartz
> ```

---

💡 **Совет**: Не коммитьте свой `API_KEY`! Используйте `.env`-файл или вводите ключ вручную в настройках.

Удачи на собеседованиях! 🚀
```

---
